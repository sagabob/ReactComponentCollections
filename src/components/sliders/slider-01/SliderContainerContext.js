import { createContext } from "react";

const SliderContainerContext = createContext({});

export const SliderContainerContextProvider = SliderContainerContext.Provider;
export default SliderContainerContext;
